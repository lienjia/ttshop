<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lej.ttshop.dao.TbItemCustomMapper" >

  <sql id="Base_Column_List" >
    id, title, sell_point as sellpoint, price, num, barcode, image, cid, status, created, updated
  </sql>
  <select id="countItems" resultType="long">
    select
    count(*)
    from tb_item
    order by id
  </select>

  <select id="listItemsByPage" parameterType="com.lej.common.dto.Page" resultType="com.lej.ttshop.pojo.po.TbItem">
    select
    <include refid="Base_Column_List" />
    from tb_item
    order by id
    limit #{offset},#{rows}
  </select>
</mapper>